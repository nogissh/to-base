version: '3'

services:
  to-base:
    container_name: to-base
    build: .
    expose:
      - 80
    environment:
      - VIRTUAL_HOST=www.toshikiohnogi.net
      - LETSENCRYPT_HOST=www.toshikiohnogi.net

networks:
  default:
    external:
      name: webproxy
